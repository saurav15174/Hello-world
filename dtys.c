#include <stdio.h>
#include <string.h>

int main()
{
	int test;
	long int n;
	scanf("%d",&test);
	while(test>=1)
	{
		char str[100001];
		scanf("%s",str);
		
		long int i=0;
		int arr[2];
		while(str[i]!='\0')
		{
			if(str[0]=='C')
			{
				if(str[1]=='E' || str[1]=='S')
				{
					printf("yes\n");
				}
				else if (str)
			}
			else
			{
				printf("no\n");			
			}
		
		i++;
	}
}